import * as React from 'react';

export default function Login(props: React.HTMLProps<HTMLDivElement>) {
    return (
        <div style={{backgroundColor:"yellow"}} {...props}>
        </div>
    )
}